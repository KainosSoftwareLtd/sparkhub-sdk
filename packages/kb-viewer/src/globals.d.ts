// CSS imports (e.g. @blocknote/mantine/style.css) are emitted verbatim into
// dist/ and handled by the consumer's bundler (Next.js, Vite, …). This
// declaration only satisfies tsc.
declare module "*.css";
